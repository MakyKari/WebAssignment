<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .positive-difference {
            color: green;
        }
        .negative-difference {
            color: red;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
          <a class="navbar-brand" href="#"><img src="/stock.png" alt="Stock Movement Icon" width="80" height="80" class="d-inline-block align-top"></a>
      
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
      
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <h3>
                    <a class="nav-link active" href="/">Home</a>
                </h3>
              </li>
            </ul>
            <h3>
            <ul class="navbar-nav ml-auto">
              <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <%= profile.name %>
                    </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <a class="dropdown-item" href="/login">Exit</a>
                  <a class="dropdown-item" href="/profile">Profile</a>
                </div>
              </li>
            </ul>
        </h3>
          </div>
        </div>
      </nav>

      <div class="container mt-5">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">User Profile</h5>
                        <p class="card-text">Username: <%= profile.name %></p>
                        <h6 class="card-subtitle mb-2 text-muted">Tickers:</h6>
                        <ul class="list-group">
                            <% if(profile.tickers && profile.difference) { %>
                                <% for(let i=0; i<profile.tickers.length; i++) { %>
                                    <li class="list-group-item d-flex justify-content-between align-items-center">
                                        <%= profile.tickers[i] %>
                                        <% if(profile.difference[i] > 0) { %>
                                            <span class="positive-difference"><%= profile.difference[i].toFixed(2) %>%</span>
                                        <% } else if(profile.difference[i] < 0) { %>
                                            <span class="negative-difference"><%= profile.difference[i].toFixed(2) %>%</span>
                                        <% } else { %>
                                            <span><%= profile.difference[i].toFixed(2) %>%</span>
                                        <% } %>
                                    </li>
                                <% } %>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <div style="margin-top: 10px; display: flex;  justify-content: center;">
        <footer>Aisultan Tabuldin SE-2201</footer>
    </div>
</body>
</html>